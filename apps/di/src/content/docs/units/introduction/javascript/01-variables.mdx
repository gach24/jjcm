---
title: Variables
description: Declaración de variables en JavaScript con let, const, var y buenas prácticas actuales.
evaluations:
  - id: RA1
    statement: Genera interfaces gráficos de usuario mediante editores visuales utilizando las funcionalidades del editor y adaptando el código generado
    criteria:
      - Se han analizado las herramientas y librerías disponibles para la generación de interfaces gráficos.
      - Se ha creado un interfaz gráfico utilizando las herramientas de un editor visual.
    contents:
      - Generación de interfaces de usuario
sources:
  - author: Fernando Herrera
    url: https://cursos.devtalles.com/courses/javascript-moderno
  - author: Juan Pablo De la torre Valdez
    url: https://www.udemy.com/course/react-de-principiante-a-experto-creando-mas-de-10-aplicaciones/
sidebar:
  order: 1
date: 2026-03-21
updated: 2026-09-16
author:
  name: Germán Carreño
  email: germanach@educastur.com
  url: https://github.com/gach24
  image: https://github.com/gach24.png
  social:
    - name: GitHub
      url: https://github.com/gach24
---

import HeaderBadge from "starlight-ion-theme/components/HeaderBadge.astro";
import {
	Aside,
	Card,
	CardGrid,
	LinkCard,
	Steps,
	TabItem,
	Tabs,
} from "@astrojs/starlight/components";


JavaScript es **dinámico** y **débilmente tipado**: una variable puede referirse a un string y más tarde a un número u otro valor. Eso no invalida las buenas prácticas de nombrado ni el uso de herramientas como TypeScript en proyectos reales.

<CardGrid stagger>
	<Card title="let" icon="pencil">
		Declaración **reasignable**. Ámbito de **bloque**. Existe **zona muerta temporal** (TDZ) hasta la línea de declaración.
	</Card>
	<Card title="const" icon="approve-check">
		**No reasignable** el enlace (la “etiqueta” del valor). Debe **inicializarse** al declarar. Ámbito de bloque y TDZ igual que `let`.
	</Card>
	<Card title="var" icon="warning">
		Estilo **heredado**. Ámbito de **función** y hoist peculiar. Evitar en código nuevo; sigue existiendo en legado y ejemplos antiguos.
	</Card>
</CardGrid>

## `let`

Puedes declarar sin valor inicial: el identificador existe, pero su valor es `undefined` hasta la primera asignación.

```js {1, 6}
let cliente = "Germán";
cliente = "Ana";
cliente = 20;
console.log(cliente); // 20

let precio; 
console.log(precio); // undefined
precio = 20.5;
console.log(precio); // 20.5
```

<Aside type="tip" title="Comillas y plantillas">
	Como veremos más adelante podemos utilizar strings: con `"..."`, `'...'` o *template literals* `` `Hola ${nombre}` ``.
</Aside>

## `const`

Con `const` **no** puedes reasignar el nombre a otro valor. Eso no congela objetos ni arrays: solo impide que `persona` apunte a **otra** referencia.

```js {1, 5}
const cliente = "Germán";
// cliente = "Ana"; // TypeError en modo estricto habitual

const persona = { nombre: "Ana", edad: 30 };
persona.edad = 31; // válido: mutamos el objeto
// persona = {};    // inválido: reasignación del enlace
```

Debes dar valor en la misma sentencia de declaración:

```js
// const sinUsuario; // SyntaxError
const sinUsuario = null;
```

## `var`

- **Ámbito de función**, no de bloque: un `var` dentro de un `if` o un `for` “se escapa” a toda la función.
- **Hoisting**: la declaración se eleva y puedes obtener `undefined` antes de la línea que escribiste, en lugar de un error claro.
- En **módulos ES** y en proyectos actuales, `let`/`const` son la norma.

```js
function ejemplo() {
	if (true) {
		var x = 1;
		let y = 2;
	}
	console.log(x); // 1 — aún visible
	// console.log(y); // ReferenceError
}
```

<Aside type="caution" title="Legado">
	Si mantienes o estudias código antiguo, verás `var`; para lo que escribas tú, **`const` por defecto** y `let` cuando necesites reasignar.
</Aside>

## Ámbito de bloque

`let` y `const` viven en el bloque `{ ... }` donde se declararon.

```js {4, 6}
{
	// console.log(a); 
	let a = 1;
	console.log(a); // 1
}
// console.log(a); // ReferenceError — fuera de ámbito
```

## Convención práctica

<Steps>

1. Usa **`const`** para todo enlace que no vaya a reasignarse (casi siempre al principio).
2. Si necesitas reasignar (`suma = …`, contadores, lectura en bucles clásicos), usa **`let`**.
3. No uses **`var`** salvo requisitos muy concretos de compatibilidad o lectura de código viejo.

</Steps>

## Nombres de variable

Reglas habituales (como en cualquier lenguaje):

- Letras, dígitos, `_` y `$`; **no** puede empezar por dígito.
- Evita palabras reservadas (`let`, `class`, `return`, …).
- Convención muy extendida: **camelCase** para variables y funciones (`precioBase`, `obtenerUsuario`).

## Comparar `let` y `const` en un vistazo

<Tabs>
	<TabItem label="let">
		```js
		let contador = 0;
		contador += 1; // ok
		let texto = "hola";
		texto = "adiós"; // ok
		```
	</TabItem>
	<TabItem label="const">
		```js
		const MAX = 100;
		// MAX = 200; // TypeError

		const lista = [1, 2];
		lista.push(3); // ok — mismo array
		// lista = []; // TypeError
		```
	</TabItem>
</Tabs>

## Ejercicios

Resuélvelos en `src/01-variables/exercises.js` y comprueba cada resultado en la consola del navegador con Live Server. Si aún no tienes el repositorio, vuelve a [Preparar el proyecto](/jjcm/di/units/introduction/javascript/00-project-setup/).

### Básicos

1. Declara con `let` una variable `nombre` y asígnale tu nombre. Muéstrala en consola, reasígnale otro nombre y vuelve a mostrarla.
2. Declara una constante `PI` con el valor `3.1416`. Intenta reasignarla, observa el error en consola y **deja la línea comentada** con el nombre del error que se produce.
3. Declara una variable `edad` sin valor y muéstrala. Asígnale después un número y muéstrala de nuevo.

	Salida esperada:

	```txt
	undefined
	25
	```

4. Declara `precioBase` con valor `100`, `iva` con `0.21` y calcula `precioFinal`. Decide para cada una si debe ser `let` o `const` y justifícalo en un comentario.

### Para pensar

5. Predice qué imprime este código **antes** de ejecutarlo. Escribe tu predicción en un comentario y compárala con la consola.

	```js
	let x = 1;
	{
		let x = 2;
		console.log(x);
	}
	console.log(x);
	```

6. Predice de nuevo y explica en un comentario por qué la última línea falla si `lista` es una constante.

	```js
	const lista = [1, 2];
	lista.push(3);
	console.log(lista);
	lista = [];
	```

7. Estas variables tienen nombres inútiles. Renómbralas siguiendo la convención **camelCase** de forma que el nombre diga qué contienen.

	```js
	let a = "Ana";
	let b = 30;
	let c = true;
	let d = 1250.5;
	```

### Reto

Ejecuta este fragmento tal cual. `setTimeout` solo retrasa el `console.log`, no necesitas entenderlo todavía.

```js
for (var i = 0; i < 3; i++) {
	setTimeout(() => console.log(i), 0);
}
```

Anota qué imprime. Cambia `var` por `let`, vuelve a ejecutar y anota la nueva salida. Explica en un comentario, usando lo que has visto sobre **ámbito de bloque**, por qué cambia el resultado.

### Entrega

Cada apartado se entrega con **dos commits**: uno para los ejemplos que has copiado y probado, otro para tus ejercicios. Así el historial deja claro qué es material de referencia y qué es trabajo tuyo.

<Steps>

1. Copia en `src/01-variables/examples.js` los ejemplos de este apartado que hayas probado y confírmalos:

	```bash
	git add src/01-variables/examples.js
	git commit -m "Añade ejemplos de variables"
	```

2. Resuelve los ejercicios en `src/01-variables/exercises.js`, documéntalos en `src/01-variables/README.md` y confírmalos en un commit aparte:

	```bash
	git add src/01-variables/exercises.js src/01-variables/README.md
	git commit -m "Resuelve ejercicios de variables"
	```

3. Sube ambos commits a GitHub:

	```bash
	git push
	```

</Steps>

<Aside type="tip" title="¿Un commit por ejercicio?">
	Si un ejercicio te ha costado o has probado varias soluciones, no pasa nada por hacer más de un commit. Lo importante es que **cada commit deje el archivo en un estado que funcione** y que el mensaje diga qué has hecho.
</Aside>

## Siguientes pasos

Documentación de referencia en **MDN** (español), comprobada:

<CardGrid stagger>
	<LinkCard
		href="https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Statements/let"
		title="Instrucción <code>let</code>"
		description="Ámbito de bloque, TDZ y diferencias frente a <code>var</code>."
		target="_blank"
		rel="noopener noreferrer"
	/>
	<LinkCard
		href="https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Statements/const"
		title="Instrucción <code>const</code>"
		description="Enlace no reasignable, obligatoriedad de inicializar y mutación de objetos."
		target="_blank"
		rel="noopener noreferrer"
	/>
	<LinkCard
		href="https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Statements/var"
		title="Instrucción <code>var</code>"
		description="Ámbito de función, hoisting y comportamiento histórico."
		target="_blank"
		rel="noopener noreferrer"
	/>
</CardGrid>

{/* Oculto hasta que el repositorio esté publicado. Para mostrarlo, quita este comentario.
<Aside type="tip" title="Ejemplos ejecutables">
	En el repositorio [dam-di-javascript](https://github.com/IESJJCM/dam-di-javascript) tienes este apartado en [`src/01-variables/`](https://github.com/IESJJCM/dam-di-javascript/tree/main/src/01-variables): `examples.js` con el código de los apuntes, `exercises.js` con los ejercicios y su `index.html` para abrirlo con Live Server.
</Aside>
*/}
